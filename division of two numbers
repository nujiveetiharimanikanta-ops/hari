    #include <stdio.h>

int main() {
    int num1, num2, result;

    // Read two integers from input
    scanf("%d", &num1);
    scanf("%d", &num2);

    // Perform integer division
    result = num1 / num2;

    // Print the result
    printf("%d
", result);

    return 0;
}
